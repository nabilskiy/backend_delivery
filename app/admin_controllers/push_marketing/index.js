const create_message = require('./create_message');
const get_all_messages = require('./get_all_messages');
const delete_message = require('./delete_message');
const send_message = require('./send_message');

module.exports = {
    create_message,
    get_all_messages,
    delete_message,
    send_message
};
